hola que tal youtube videntes bienvenidos una vez más a una nueva entrega desde su curso de linux desde cero vamos a continuación a entrar a trabajar con los procesos en linux vamos a ver de manera introductoria cómo son los procesos en linux qué características tienen para luego más adelante pues iré viendo con un poco más de detalle comandos situaciones que podemos utilizar para ver estos procesos entonces qué es un proceso en linux pues un proceso en linux es un proceso como en cualquier sistema operativo es una instancia de un programa que se está ejecutando dentro de nuestro sistema operativo cuando yo abro un programa un editor de texto un correo electrónico un proceso de backup normalmente son programas que al final lo que hacen es que se instancia es decir se ejecuta una instancia dentro de ese entorno hay que tener en cuenta que el linux es multitarea y multiusuario por lo tanto puede ejecutar múltiples procesos de forma simultánea dentro del mismo sistema operativo es decir dentro de uno o una instalación de escritorio de linux nos vamos a encontrar con un conjunto de procesos muy amplios que van a empezar desde los procesos más básicos del sistema relacionados con el kernel hasta ya los procesos de usuario final entonces cada proceso va a tener asignado normalmente una cpu una parte del cpu una parte de la memoria va a tener un juego de instrucciones y todas las variables que necesite y va a tener todo lo necesario para poder llevar a cabo su trabajo es una cosa muy importante en linux es que los procesos tienen una estructura jerárquica de padre-hijo es decir que normalmente cuando estamos trabajando con un proceso si necesitamos otro proceso adicional se crea un hijo esto luego veremos que recibe el nombre de fork al final vamos a ver un poco cómo es esta estructura jerárquica y bueno tenemos distintos estados de procesos en linux normales los más habituales son los procesos que se ejecutan en modo normal tenemos los procesos de tipo the ground que se estarán ejecutando en segundo plano estos generalmente son servicios del sistema también tenemos los procesos zombie que esto aunque suene raro no es nada raro esto aparece de vez en cuando en el sistema operativo y realmente su estado significa que aunque han sido eliminados aunque han sido borrados todavía existen trazas de ellos en la lista de procesos es decir se visualizan ejecuciones de los mismos incluso cuando éstos ya han sido detenidos es un poco complicado tratar de entender por qué se quedan estos procesos zombie corriendo en nuestro sistema operativo pero ya eso es algo de muy ingeniería de software tratar de pues ponernos a indagar sobre eso las propiedades que tiene un proceso en linux son más o menos las mismas para todo sistema operativo un proceso tiene un pib que es un número de proceso identificativo un código único esto es un identificativo un código que lo hace único durante todo su trabajo dentro del sistema también tenemos otros datos como el uso al que pertenece el grupo es decir un poco los datos de propiedad de ese fichero un proceso siempre tiene el el pp y de que es el proceso padre todo proceso en linux absolutamente todo tiene que tener un proceso padre excepto el proceso uno que es el primero luego esto lo veremos y claro todos los procesos en linux tienen un contexto un contexto en donde está en el espacio de dirección de las variables el código las asignaciones de memoria etcétera esto sería digamos los datos básicos de ese proceso entonces como les comentaba a todos los procesos en linux tienen una estructura jerárquica de modo que siempre tienen algún padre salvo el proceso uno que es el sistema y que también se llama init en algunos sistemas operativos y normalmente este proceso en los linux de las distribuciones más actualizadas se llama system de este es el primer proceso que arranca y desde el cual luego van a arrancar todos los demás esto es un ejemplo más adelante vamos a ver ya esto detallado con los procesos de carne pero aquí vemos que se ha arranca el sistema que es el inicial y luego tenemos otros procesos que luego se subdividen en otros procesos entonces esto crea una estructura jerárquica de apertura de procesos este concepto en linux se le denomina work of working es decir cuando un proceso padre tiene que crear un hijo automáticamente va a ser una llamada fork que va a implicar también pasarle determinados datos como por ejemplo el usuario la cruz el grupo la pila y una serie de componentes que debe heredar el proceso hijo entonces por ejemplo nos imaginamos que esto es muy se le está bajando la shell y hacemos un ls para listar el contenido entonces la shell sería el proceso padre y el ls sería el proceso hijo o sea que para que se ejecute de el ls la shell lanza un fork como diciéndole al sistema mira voy a lanzar un proceso hijo que debes ejecutar entonces todo todo absolutamente dentro de los procesos que se ejecutan en se maneja con esta relación de proceso padre hacia proceso hijo más adelante vamos a ver ejemplos de todo esto que estamos detallando ahora y bien entonces ya dentro de nuestro sistema operativo vamos a ver algunos de los comandos que nos permiten trabajar con los procesos entonces el comando más habitual que se suele utilizar es el comando ps que significa process status este comando si le damos aquí a enter podemos ver los procesos que se están ejecutando dentro de nuestra propia shell entonces podemos comprobar aquí que aparece la shell el base y luego un proceso que se llama ps que es el mismo proceso que ejecutamos aquí fíjense como linux nos dice que para ver esta información aquí él está ejecutando un proceso que se llama ps y miren aquí como nos dice mira ejecuta este una consulta con un proceso que se llama ps eso es lo que significa esta línea de acá que está viendo este comando que ejecutamos aquí entonces dentro de las propiedades o campos que nos visualiza el comando tenemos el pib aquí está todo proceso en linux como habíamos dicho tiene un número de identificación un property identificación aquí están los de estos procesos entonces aquí está el tt y que es el terminal desde el cual estamos trabajando que es nuestra shell y el tiempo de ejecución y luego está el comando propia de propiamente dicho entonces si quisiéramos un tipo más detallado de información yo escribo p ese espacio - efe ps - efe nos da información es un poco más detalladas nos dice aparte del pib el pp aid y ese es el proceso parent aid y el proceso padre luego vemos aquí otra serie de datos que lo vamos a ver más adelante y entonces aquí vemos claramente la relación que hay entre estos dos procesos es decir el proceso ps - efe tiene este pide que este es su proceso identificador y este es el proceso padre el que lo ha generado como estuvimos hablando ahorita todo proceso es hijo de un proceso superior hasta llegar al proceso o hasta llegar el proceso primero que es el sistema de entonces aquí vemos que el proceso padre 1772 es la base y aquí está el proceso padre 1772 aquí el proceso ps es hijo de ese proceso por lo tanto aparece en el campo de roses parent haití y aquí está 1772 entonces una vez que ya hemos visto los comandos básicos de ps vamos a verlo ahora en su forma más completa tenemos otra opción que es ese espacio menos l cuando vamos a intervenir que nos da aún más datos de los procesos en linux por ejemplo en este caso podemos ver algunos campos adicionales como el flat aquí está que nos da un número de acuerdo al tipo de proceso en la documentación tendríamos que ver aquí hace referencia a estos números pero para hacernos una idea por ejemplo si aquí viéramos un 4 indicaría que el proceso se está ejecutando con un súper usuario o con permisos superiores en este caso estamos viendo 0 porque lo que estamos usando es nuestro usuario final no nuestro superusuario entonces aquí está ese significa estado el state para que nos hagamos una idea el estado de un proceso puede tener distintos caracteres aquí podemos ver por ejemplo que está ese significa que el proceso está suspendido y que es interrumpible es decir está esperando que se complete algún evento y esta red de acá significa que está en ejecución por lo tanto cuál es el que está en ejecución nuestro comando ps que es el que ejecutamos acá con la opción menos l pero el proceso es ps hay algunos otros valores que podríamos ver aquí en la s de state de estado podríamos decir que si hubiéramos una zeta significa que es el proceso zombi del que les hablé anteriormente aquí si pone por ejemplo una equis es cuando matamos un proceso que significa proceso muerto más adelante vamos a ver cómo hacer eso si hubiera una w esto indica que es un proceso paginado es decir que no es válido para el kernel de linux todo esto lo vamos a ir detallando más adelante y vamos a ir viendo estos estados que les estoy comentando por ahora simplemente vemos que estos dos procesos uno tiene el estado suspendido que es ese y el otro tiene el estado running o en ejecución que es el ps que aquí fue donde lo ejecutamos entonces aquí están los otros detalles el usuario al que pertenece el pp y de upyd el tiempo de cpu la prioridad que tiene este de acá que está varía de acuerdo a como linux lo administre si es mayor si es menor esta opción de acá que es el nivel de prioridad que se le asigna al proceso aquí está la memoria y esto como lo sabemos ya es la terminal que se está ejecutando como vemos p ese es uno de los comandos que más opciones tiene dentro de linux digamos que yo quiero ver por ejemplo todos los procesos activos dentro de mi sistema linux hay una opción que es escribiendo p ese espacio - efe si yo le doy a enter aquí pues tengo un problema y es que como hay tantos procesos corriendo en linux los lanza todos hacia abajo a medida que se van ejecutando entonces lo que haríamos sería lo siguiente limpiamos nuestra terminal y aquí escribimos ps - efe colocamos tubería les aquí le damos a enter y por si no recuerdan este comando que acabamos de colocar es un comando que nos permite paginar la salida de datos que solicitamos de manera que podemos irle dando página a página entonces aquí lo que vemos es varias cosas interesantes y es que el pcf es un formato de salida de datos en linux más adelante vamos a ver otro más ya que ps tiene una cantidad enormes de formatos para sacar campos de distintos tipos pero colocando efe lo que le estamos diciendo que nos traiga un full es decir todos los procesos del sistema y que nos lo presenta en forma de paginación entonces aquí lo que estamos viendo es que nos los está trayendo colocando el wii del pib y el pp y de entonces aquí vemos algo muy interesante y es que él pide el proceso inicial es el sebin barra init cuando estuvimos hablando ahorita el proceso init o el sistema que es el que arranca nuestro sistema dependiendo de nuestra versión al día de hoy prácticamente todos utilizan el sistema de iu para verlo un poco más claramente vamos a darle aquí a nuestra este botón de opciones vamos a abrir ventana nueva y aquí vamos a escribir ls menos l y escribimos barra s bin barra y net y le vamos a dar enter entonces aquí podemos comprobar que el sebin barra y ni es un enlace de los que hemos visto a lo largo del curso que está apuntando a system de aquí lo vemos está un poco por compatibilidad para que siga siendo igual que versiones anteriores el proceso in it ahora se ha convertido en system y voy a cerrar mi nueva ventana entonces podemos comprobar que el pib 1 y el 2 que este es un poco el ine y este es el que gestiona los hilos los read del kernel salen del proceso 0 su proceso padre es el 0 este proceso en realidad no existe ya que es el arranque del sistema operativo pero está etiquetado así como 0 e igualmente podemos comprobar entonces que a partir del proceso 1 o del proceso 2 se van lanzando los procesos iniciales prácticamente es igual en todos los linux vemos que normalmente los procesos que tienen por acá como un corchete estos son procesos internos del sistema entonces mientras más vamos avanzando hacia adelante podemos ver que todos estos procesos del sistema han sido arrancados por el pay de 2 aquí lo vemos como les dije si están usando versiones anteriores a la más reciente de cualquier linux es muy probable que aquí les esté apareciendo en algunos casos el 1 como proceso padre pero como depende del kernel y hay tantos desde que salieron las primeras versiones de linux pues pueden ver lo distinto pero si seguimos hacia abajo podemos ver que hay un montón de procesos internos que son arrancados por el proceso número 2 y luego vemos que ya los procesos del linux propiamente dicho el padre casi siempre es el proceso 1 el link aquí los vemos esto es lanzado por todos los comandos que ejecutan el resto del sistema si seguimos viendo vemos que hay un montón de procesos que descienden de él in it estos procesos son muy internos y son muy orientados a la administración de linux pero con esto podemos ir conociendo la estructura de padre-hijo en los procesos de linux es decir que al final vamos confirmando la jerarquía de procesos en donde podemos identificar quién es el padre y quién es el hijo hasta llegar al proceso número 1 y bien eso va a ser todo por el momento en esta parte introductoria a los procesos en linux esta es nuestra primera vista a este tema de aquí en adelante vamos a ir trabajando con todos los relacionados a procesos así que nos vemos en una siguiente entrega gracias por su atención y hasta pronto
