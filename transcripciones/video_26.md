hola que tal youtube videntes bienvenidos una vez más a una nueva entrega de este su curso de linux desde cero vamos a iniciar trabajando con los permisos y los usuarios la administración de permisos y la administración de usuarios en linux todo el tema de usuarios grupos permisos y demás primero vamos a empezar verificando el tema de los permisos para entender y aterrizar ciertos conceptos que hay que tener claros y más adelante entonces vamos a ver opciones para crear usuarios grupos etcétera entonces por ahora vamos a trabajar con los permisos y comprobar cómo funcionan estas reglas en linux entonces todo usuario que es creado en el sistema tiene un nombre y una clave este nombre y esta clave son las que les permiten ingresar entrar dentro del sistema además por otro lado los usuarios pertenecen a grupos un grupo que no es nada más que un conjunto de usuarios que tienen algún perfil en común y dentro de linux entonces tenemos voy a escribir acá les va pas w y voy a entrar aquí este fichero dentro de linux contiene todos los usuarios que existen creados dentro del sistema cada uno de ellos con su nombre y lo que se denomina un y de aquí podemos ver por ejemplo en este usuario y usher 001 que es el nombre del usuario aquí está su clave que la vemos solamente con una equis ya que esto está encriptado aquí entonces vemos el id de este usuario y otros datos que veremos más adelante en este caso nos interesa de sobremanera el ide del usuario y el grupo al que pertenece que también se maneja con un idh es decir este usuario pertenece al grupo 1000 entonces aquí está el directorio home dónde está ese usuario y la base con la que está trabajando entonces si nos vamos aquí en la parte superior vemos un usuario que se llama root que tiene un y de cero pertenece al grupo cero su home es barras route y está usando entonces la misma base que el usuario y usher 001 entonces los usuarios cuando se crean se les asigna un pide y se les suele asignar un grupo o varios grupos existe también otro directorio si aquí colocamos barra etc brou y nos dice permiso denegado que es por qué no colocamos la instrucción les less etc barra grow aquí linux nos permite ver los grupos que ya están creados en el sistema entonces aquí lo que vemos es el nombre del grupo y el ide como vemos el grupo root tiene un y de cero no podemos confundir grupo con usuario es decir hay un grupo de usuarios que se llama root y también hay un usuario que se llama root consecuentemente el usuario root pertenece al grupo del mismo nombre que es ruth esto hay que tenerlo claro de igual modo el usuario también puede pertenecer a cualquier otro grupo si nos vamos aquí al fondo vemos a nuestro usuario y usher 001 que pertenece al grupo de usuario y un ser 001 pero también este usuario podría pertenecer al grupo y usar 001 002 etcétera entonces teniendo esto en cuenta uno de los primeros comandos que podemos ver es el comando aid y este comando hoy día es un comando muy clarificador porque nos dice que y detiene un usuario a qué grupo pertenece este usuario y otros grupos adicionales a los que pertenece este usuario como vemos este usuario y usher 001 pertenece a siri room floppy vídeo etcétera por defecto la consulta que hacemos en la ruta etc password sólo podemos ver el grupo principal pero podemos observar que un usuario puede pertenecer a múltiples grupos esto de los grupos es muy importante lo vamos a ir viendo más adelante porque el grupo determina los posibles permisos de un usuario sobre los ficheros entonces más adelante vamos a estar detallando todos los temas de creación de usuario modificación de usuario asignación de usuarios a grupos y temas de este estilo que son llamadas de administración los vamos a ir detallando más adelante cuando estemos viendo el tema de gestión de usuario entonces vamos a recordar cuando hacemos ls espacio menos el uno de los datos que nos trae este comando es el usuario y el grupo propietario del fichero o directorio en cuestión qué es lo que vamos a estar viendo durante los próximos capítulos por lo tanto aquí si yo hago un ls menos el de mi directorio barra de mp puedo ver que hay ficheros que pertenecen a ellos el 001 otros que pertenecen a ruth y si sigo mirando aquí dándole flecha hacia arriba cambio de directorio y escribo un s de barra bing puedo ver más o menos lo mismo usuarios grupos etcétera ahora mismo no tenemos muchos usuarios configurados porque lo nuestro linux es recién instalado pero más adelante vamos a crear unos usuarios de prueba para poder ver los ejemplos más detallados entonces es importante que entiendan estos conceptos de usuarios y grupos que es muy parecido a lo que hay en windows aunque el concepto es un poco más plano más sencillo porque esto no se ha modificado desde que instalamos nuestro sistema e igualmente tampoco el protocolo ha sido diferente desde la creación de linux ha sido todo lo mismo bien entonces aquí voy a hacer un clic de mi terminal y entonces vamos ahora a profundizar en los permisos vamos a ver cómo podemos ver los permisos que tienen los ficheros y directorios y qué significa cada uno de ellos aunque hay algunas técnicas más potentes para trabajar con permisos como son los azules o listas de control de accesos y los s linux que ya pues no forman parte de este curso porque eso está orientado ya a administración de servidores es más de administración pero con lo que vamos a aprender aquí pues vamos a poder aterrizar estos conceptos y luego ustedes pueden ampliar los conocimientos para aplicarlos en procedimientos más entonces para saber los permisos que tiene un fichero en linux el comando más importante es ls espacio menos el este ls con cualquiera de las opciones nos es útil si podemos ver esta parte en los resultados que nos trae este comando ya estuvimos viendo un preview del detalle de la información que aparece en esta columna en vídeos anteriores cuando estuvimos viendo la introducción a ls hablábamos de que el primer carácter de todos estos caracteres es el tipo de fichero digamos no los que tienen guión son ficheros normal los que tienen de son directorios los que tienen l son enlaces y los que tienen be son bloques igualmente los que tienen se son un dispositivo de tipo caracter entonces este primer carácter está claro esta letra de identificar el tipo que tenemos y luego tenemos entonces nueve letras que van en grupos de tres estas letras están las tres primeras son los permisos para el propio usuario ahora veremos qué significan las tres siguientes identifican los permisos para el grupo al que pertenece el fichero y el resto sería otros es decir usuarios que no pertenezcan a grupos del sistema es decir que estos tres primeros caracteres identifican que permiso tiene este usuario el usuario cuyo ser 001 los tres siguientes identifican cualquier usuario que pertenezca al grupo y user 001 y las tres últimas son los permisos para cualquier otro usuario que no sea propietario ni que pertenezca a ningún grupo entonces cada una de estas tres letras identifica un comportamiento en concreto si la letra está indica que el permiso está activo si en lugar de letra hay un guión como lo vemos acá indica que ese permiso de esa sección está desactivado está inactivo es decir que sí aparecen las tres letras indica que el usuario tiene todo activado y si no dónde es un guión indica permiso desactivado entonces qué significa esta r la w y la equis porque la estamos viendo que se repite en varias veces bueno la primera la r significa que un fichero se puede leer se puede listar copiar o visualizar es decir que en este fichero yo puedo hacer un cut un ls etcétera etcétera la segunda letra la w indica que se puede modificar o borrar el contenido de este fichero incluso cambiar cambiar de permisos está w que significa raid y la r que significa read de escritura pero no solo es que se puede escribir en el fichero sino que también puedo borrarlo es decir cualquier operación que realice una modificación en el elemento entonces está x indica ejecución estas tres letras identifican esos comportamientos por ejemplo este fichero que está acá documento uno tiene estos permisos entonces vemos que es un fichero porque empieza por un guión luego la erre nos dice que este fichero tiene permiso de lectura y la w que el usuario tiene permiso de escritura entonces el hecho de que no esté aquí la equis no quiere decir que el usuario no tenga permisos para ejecutarlo porque en el fichero pertenece al usuario este guión lo que significa es que este fichero no es ejecutable es decir la ausencia de la equis no significa que el usuario u otro usuario con acceso no lo pueda ejecutar sino que este fichero no es un ejecutable es un fichero de texto que se abre por lo tanto aquí no puede haber una equis porque no tiene esa función de ejecución luego entonces para el grupo solo tengo permiso de lectura está r que aparece aquí está esta segunda r indica que todos los usuarios dentro de user 0 1 dentro de este grupo sólo tienen permiso de lectura los usuarios que pertenecen solo lectura no pueden modificar ni actualizar ni borrar entonces al resto de usuarios que acceden al sistema les pasa lo mismo hay una r aquí en el último grupo lo que indica que sólo lo pueden leer al final este fichero solo es modificable por el usuario propietario y eso lo indica está w de aquí entonces vamos a irnos vamos a hacer un clear vamos a ir a nuestro directorio tmp ls - el inter aquí bueno no tenemos básicamente ningún fichero que nos sirva para el ejemplo vamos a cambiar de directorio vamos a irnos al barra 'seré barra ping a ver si encontramos alguno el ese espacio menos el bueno aquí entonces si vemos otros ejemplos como estamos viendo este fichero que si es un ejecutable es un binario vemos que el usuario si tiene permisos de lectura escritura y ejecución los usuarios de el grupo que en este caso es ruth pueden ejecutar y leerlo y de igual modo pueden estarlo y lo mismo todos los usuarios que tengan acceso a este fichero como vemos aquí hay distintas funcionalidades dependiendo de los usos vamos a hacer un clear vamos a irnos el directorio va a robar barra lo que hacemos l ese espacio menos l pues aquí tenemos entonces más de lo mismo en mi caso por ejemplo aquí hay un kern punto log imagino que ustedes también deben estar visualizando los estos dos archivos aquí vemos entonces que el propietario puede leer y escribir y que el grupo puede leer y que en cambio el resto de usuarios que acceden no pueden hacer nada incluso les daría un permiso denegado si intentaran hacer alguna ejecución sobre este archivo ya que aquí no tienen los permisos bien entonces limpiamos nuestra terminal ya hemos visto entonces cómo son los permisos para los ficheros entonces vamos a hacer otro ls menos l comprobamos entonces que los directorios al igual que los ficheros tienen la misma estructura de letra y permisos para el usuario permisos para el grupo y permisos para otros pero claro los ficheros y los directorios son distintos entonces la definición y cada tipo de permiso debe ser diferente entonces ya habíamos dicho que la s significa que la ejecución en los ficheros permite leer los mismos y que la equis permite ejecutar entonces en un directorio lo que identifica la equis es que se puede ver el contenido es decir yo puedo hacer ls de los directorios que tengan esta x acá y acá si no tuviera permisos no se pudiera hacer el ls dentro de para ver el contenido de estos directorios la w para directorios lo que significa es que podemos modificar o borrar archivos y directorios dentro de el mismo es decir que cuando yo tengo esta w acá para un directorio indica que yo puedo escribir o sea crear borrar o modificar o eliminar incluso ficheros dentro de estos directorios pero lo podemos hacer solamente dentro de el directorio y es posible que el contenido de los ficheros que estén dentro de estos directorios no nos permitan realizar estas acciones pero lo que es el directorio con esta w linux lo que nos permite es manipular los ficheros que están dentro de estos directorios e incluso crearlos pero luego dentro de estos directorios es posible que haya un fichero que contenga permisos que no nos permitan realizar acciones directamente sobre ellos en resumen tenemos un primer permiso para el directorio y luego tenemos entonces los permisos para los ficheros dentro de estos directorios entonces la equis para los ficheros lo que significa es que básicamente podemos entrar en las carpetas si no hay una equis aquí podemos hacer ciertas operaciones pero no podemos entrar dentro de esta carpeta a realizar otras operaciones entonces vamos a limpiar nuestra terminal y luego de haber aclarado y lo vamos a dejar hasta aquí para no extendernos y más adelante entonces ya vamos a empezar a trabajar con la creación modificación y actualización de permisos en ficheros y directorios de linux así que nos vamos a ver en una siguiente entrega gracias por su atención
