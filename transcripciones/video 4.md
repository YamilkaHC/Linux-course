hola que tal youtube videntes bienvenidos una vez más a una nueva entrega de este su curso de linux desde cero en el día de hoy vamos a profundizar acerca de los editores de texto en linux y vamos a realizar una introducción sobre los ficheros el sistema de ficheros en linux en linux con los editores de texto podemos trabajar con la creación y modificación de scripts y qué es un script bueno un script es un fichero de texto que contiene comandos de unix linux para que sean ejecutados por una shell se trata de un intérprete de comandos que realiza lectura de ficheros cuando se ejecuta un script lo primero que hace el shell de nuestro sistema operativo es averiguar para qué se le fue escrito este programa entonces la selva' a ejecutar el script como entrada estándar al sistema si le estás preguntando en qué shell se debe ejecutar un script pues esto viene indicado en la primera línea del script que estamos ejecutando los editores de texto que son fundamentales para el manejo de texto en nuestro linux son programas que nos permiten crear y modificar archivos digitales compuestos únicamente por textos sin formato en linux tenemos varios editores de texto como lo son bin tenemos a new y max también sub line text bastante utilizado está atom que es muy conocido hay otros editores como por ejemplo nano gini o ebit también este es este es uno de los más conocidos en linux y también existe un editor de texto llamado kate el editor de texto debe ser considerado como una herramienta de trabajo del programador o el administrador de una máquina o de un servidor la elección de este editor depende de varios factores algunos de ellos son muy subjetivos esto quiere decir que dependiendo de lo que tú desees o de lo que quieras hacer con tu equipo puedes utilizar uno u otro y bien vamos a continuar trabajando y ahora vamos a echar un vistazo a los tipos de usuario en linux estos de tipos de usuario es muy importante aprenderlo porque dependiendo del tipo de usuario que seamos vamos a poder realizar algunas actividades u otras así sea que trabajemos como usuario final o como usuario con niveles administrativos y se puede decir que tenemos tres tipos de usuarios en linux el usuario root o administrador los usuarios normales o de trabajo y los usuarios especiales o de sistema dependiendo de qué actividad vayamos a realizar normalmente trabajaremos como usuario normal o administradores luego veremos que hay usuarios especiales con los cuales no se trabaja directamente con ellos además de los usuarios normales existe un directorio que se denomina directorio home este es un directorio privado del usuario directorio home al que desde el principio sólo el usuario debe el acceso sólo el usuario de ese directorio puede trabajar ahí y como comentamos en vídeos anteriores los usuarios deben tener una shell para trabajar para acceder al sistema operativo desde la cual podrán trabajar y configurar y ejecutar determinados comandos y de igual modo vamos a ver que éstos pueden pertenecer a un grupo deben pertenecer a un grupo más adelante entonces vamos a estar viendo el tema de los usuarios de grupo como darles de alta como trabajar con ellos etcétera y veremos cómo se les indica que deben estar al menos en un grupo el usuario root que es el usuario root el usuario root es el usuario administrador del sistema es un usuario con todos los tipos de privilegios puede hacer cualquier cosa y por lo tanto hay algo importante con este usuario y es que no conviene trabajar con él habitualmente es decir el usuario root sólo deberíamos usarlo para tareas administrativas muy concretas de hecho a lo largo del grupo vamos a ver que hay una opción para realizar procesos como usuario root pero sin salirnos del usuario normal que estemos usando esto es para minimizar el peligro de trabajar con un usuario root ya que como usuario root podemos estropear el sistema podriamos dañar algo por lo tanto entonces hay que tener mucho cuidado el directorio de trabajo de el usuario root el home es barra root aunque esto depende mucho del sistema pero normalmente se denomina barra cuando estemos trabajando con el usuario root tenemos que tener esto en cuenta y una de las cosas que suele identificar al usuario root cuando está trabajando es que en el pro de la shell donde están los comandos aparece como un indicador una almohadilla esto ya lo habíamos visto luego tenemos los usuarios normales que es con los que se va a trabajar habitualmente si necesito hacer algo en linux voy a utilizar un usuario normal y posiblemente se va a crear un usuario root y el root es el que va a crear los otros usuarios y normalmente los directorios con el directorio privado de cada usuario se encuentra en con cualquier usuario que es creado en el sistema está en esa ruta para rajoy el concepto de directorio home es muy importante porque estos son los directorios de cada usuario luego existen directorios de datos directorios de sistema dependiendo de para que vayamos a usarlo pero donde nosotros entramos cuando vamos a la shell y trabajamos directamente por ejemplo con la terminal es con el usuario com los usuarios especiales son los que se crean para ejecutar determinados procesos del sistema puede ser el bin el diamond el el aicm el lp que se usan para por ejemplo configurar impresoras de sincronización de sistemas de parada de correos etcétera estos son usuarios que no se suelen usar para trabajar con ellos directamente sino que son usuarios que gestionan procesos de software normalmente se les denomina cuentas del sistema a veces trabajan como root como ya lo habíamos comentado anteriormente anteriormente que un usuario se puede convertir en usuario root pero tienen un par de diferencias la primera es que no tienen self es decir lo que se les es que no tienen login no se puede bloquear como usuario normal porque son usuarios que se mueven como un usuario oculto para hacer su función pero no podemos loguearnos para trabajar con ellos porque éstos no tienen self para trabajar por eso se les denomina como usuarios de sesión no login esto lo vamos a ver más adelante también y bien vamos a pasar ahora a adentrarnos en el tema de los ficheros en linux el concepto de ficheros en linux es un concepto que a veces cuesta un poco entender cuando nosotros por ejemplo venimos de otros entornos como por ejemplo de windows que es relativamente distinto y que pues a veces nos cuesta un poco pero es importante que tengamos estos conceptos claros porque son la base para poder trabajar con linux en linux o un sistema de ficheros no es nada más que la forma en que se guardan los datos dentro de el sistema lo mejor para entender cómo funciona el sistema de ficheros en linux es hacer una comparativa con windows que es me imagino el sistema operativo con el que y trabajando en este momento si estan virtualizando así que podemos ver cómo se maneja en windows y lo vamos a comparar entonces con linux y así veamos las diferencias que hay entre ellos entonces si tenemos windows y queremos crear un sistema de ficheros en un windows normal podríamos encontrarnos con el sistema que tiene un disco duro creado con varias particiones digamos por ejemplo unidad ce y unidad de windows trabajamos con letras nombrando las unidades cada letra identifica a una partición oa un disco y luego dentro de cada letra lo que tenemos básicamente es una estructura de directorios que digamos empezamos por la raíz y luego cada uno va a haber directorios y sub directorios de manera que para acceder a un determinado fichero tenemos que ir dándole la letra por donde empieza especificamos la raíz y vamos bajando por los directorios hasta llegar al fichero en windows la segunda unidad pues tiene más o menos la misma estructura la única diferencia es que cambia la letra la unidad que identifica la partición o el disco en el que estamos trabajando esto es un poco para que entonces entendamos cómo se maneja el tema de los ficheros en windows y así pues en linux lo tengamos más claro entonces en linux la cosa es distinta tenemos un disco duro con dos particiones pero en el caso de linux nos vamos a encontrar con que sólo hay un root una raíz no importa cuántos discos o cuántas particiones de discos tú tengas en tu sistema linux siempre se comienza por barra por la raíz ese es el root del sistema y a partir de ahí se cuelgan los directorios entre todas las particiones habrá que designar una definir una que sea digamos la raíz la ruta desde donde se comience todo y luego claro a diferencia de windows qué pasa con el resto de particiones bueno se supone que tenemos una segunda partición pues en el caso de linux en lugar de hacer una unidad distinta como windows que tiene una letra distinta y que está completamente separada pues se integra dentro de la estructura del directorio en este caso la partición 2 la asignamos a este directorio lo cual es un poquito distinto a windows eso quiere decir que cuando yo estoy en cualquiera de estos directorios del 1 2 y 3 me encuentro en la partición 1 pero en cuanto me sitúo en el directorio de ypf 3 físicamente estoy en la partición 2 esto tiene ventajas e inconvenientes la ventaja es que sólo tengo una estructura de directorios por la que me muevo no tengo varias como existe en windows que como tantas particiones o tantos discos tantas unidades voy a tener las desventajas son que para un usuario normal cuando entra en el directorio del 3 ese usuario no sabe que ha cambiado de partición en muchos casos pues esto es indiferente que el usuario sepa si está en un disco o en otro este es un concepto que al principio ahora pues lo vemos un poco confuso porque está un poco más orientado a lo de administración y estamos empezando pero sí es bueno entender un poco esto para que sepamos cómo funciona de ficheros y cómo podemos movernos a través del mismo para acceder a un fichero nosotros lo vamos a hacer así vemos que la barra con la que se trabaja es distinta a la de windows en windows es inclinada hacia la izquierda es esa barra que tenemos cuando presionamos shift 7 o si tenemos un teclado numérico también podemos verla al lado del asterisco es un poco extraño para los que vienen de windows y están iniciando ahora en linux pero ya veremos que es fácil de entender sobre todo si se va a ser usuario final hay que mencionar que dentro de la estructura de linux al igual que en windows hay directorios que existen dentro de el sistema los iremos viendo a lo largo del curso no todos porque muchos de ellos son para administración pero bueno en la barra que es de donde comenzamos bin que contiene ficheros binarios o ejecutables barra de que es donde están los dispositivos ya lo veremos más adelante esto que aquí se encuentran los dispositivos con los que trabajan linux los discos las impresoras la memoria etcétera el etc que casi siempre tiene ficheros de configuración el home que tiene los directorios personales de los usuarios las librerías en live el media y el mmt que es para montar cosas tanto automáticas como de manera manual el opc que se utiliza para instalar software externo como base de datos el barra pro que es un directorio muy interesante para administración esto ya lo veremos cuando estemos más avanzados el directorio home de el rup de usuario los ficheros binarios que están en el sebin el barra de mpx para los ficheros y directorios temporales el directorio usb que antes era para guardar directores de usuario pero que ahora tiene aplicaciones de archivos varios compartidos y es un directorio bastante importante que es el directorio bar donde pues se almacenan los ficheros de log las trazas y ficheros dinámicos más adelante vamos a ver la importancia de todos estos ficheros los usuarios que hemos trabajado con windows ya tenemos claro que hay una serie de sistemas de ficheros que el propio microsoft ha creado y que mantiene como son fatfat 32 exfat ntfs y cada uno de ellos tiene sus características a la hora de almacenar los datos esto no pasa en linux ya que el linux al ser abierto hay una cantidad tremenda de sistemas de ficheros algunos se preguntarán cómo podemos nosotros manejar todo este sistema de ficheros bueno pues lo normal es que los sistemas de ficheros en linux sean muy estándar y casi todas las distribuciones llevan el mismo tipo pero esto no implica para que podamos trabajar con otro sistema de ficheros ya más adelante vamos a aprender a cómo movernos a través de todos estos sistemas de ficheros que existen en linux y bien eso va a ser todo por el momento en esta parte introductoria a los editores de texto y a los sistemas de ficheros más adelante vamos a seguir trabajando lo vamos a dejar aquí por el momento y nos vemos en una siguiente entrega hasta pronto
