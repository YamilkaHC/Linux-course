https://www.youtube.com/watch?v=09ORR6vzlJY

Transcript:
hola que tal youtube videntes bienvenidos una vez más a una nueva entrega de este su curso de linux de st 0 seguimos trabajando con nuestras variables de entorno en linux variables de entorno que vienen del sistema como variables de self que tiene que ver con la shell con la que estamos trabajando vamos a ver ahora cómo podemos crear o modificar variables dentro del linux entonces la manera más rápida de crear una variable es directamente colocando la si yo coloco aquí por ejemplo uve de variable igual a 10 cuando le doy a
enter con esta simpleza ya estamos creando una variable dentro de linux y aquí comprobamos que crear una variable dentro de un sistema operativo como linux es muy fácil luego veremos que este tipo de valores o de variables de creaciones las utilizamos habitualmente en el script para ver si esto ha funcionado escribimos entonces echo y escribimos dólar ve colocando echo signo de dólar y el nombre de mi variable que acabo de crear y asignarle un valor cuando le doy a enter vemos aquí que linux nos dice el valor de mi variable
sin problemas este signo de dólar se utiliza a posteriori de crear la variable es decir cuando creamos o modificamos la variable no hay que poner el signo de dólar sino solamente el nombre de la variable y su valor por ejemplo si yo aquí escribo var igual a 20 le doy a enter y ahora digo echo dólar bar puedo ver que ya tiene su valor asignado estas variables las hemos creado intencionalmente en minúsculas aunque en los ejemplos que hemos estado viendo en linux las variables se manejan con nombres en mayúsculas las del
sistema siempre deben de ir en mayúsculas no se debe de modificar pero cuando nosotros estemos trabajando con ser script o similares pues esto va a ser perfectamente válido ponerlas en minúsculas pero para nuestros propios programas entonces digamos aquí que yo escribo print en nuestro comando que ya debemos saber lo que significa cuando le doy a enter acá e intento buscar mis variables que acabó de crear veo que no aparecen porque porque son variables de entorno es decir se han creado cuando el usuario ha entrado dentro del entorno
entonces digamos que aquí yo escribo set para ver mis variables de tipo health y aquí pues podríamos poner por ejemplo set dollar grep y escribo bar pues si vemos nuestra variable bar igual a 20 que acabamos de crear entonces aquí por ejemplo hacemos un clear y aquí escribo por ejemplo set tubería les cuando le doy a enter si vamos viendo las variables que se han creado vamos a ver si encontramos las que acabamos de crear aquí aparece una gran cantidad de caracteres por esto es tan importante colocar grep llegar a algún momento en
el que la encontremos y aquí las tenemos v igual a 10 y bar igual a 20 están etiquetadas como variables de self porque las hemos creado en la serie no se han creado en el fire meant es decir las variables que se crean al momento de entrar al sistema más adelante cuando hagamos el script aprenderemos a utilizarlas para hacer programas por ahora pues es suficiente ver con esto cómo se crean variables en linux letra q y estas variables también las podemos cambiar si yo coloco por ejemplo uve iguala y colocó aquí por ejemplo varias
equis y aquí abajo entonces escribo echo dólar uve cuando le damos a enter vemos entonces que ha cambiado el valor anterior que tenía esta variable que era un número 10 por lo tanto pues nos damos cuenta que linux acepta cualquier cosa más adelante sería entonces ya a criterio del usuario determinar si el valor es correcto o no bueno esto es un pequeño ejemplo de cómo podemos utilizar y configurar nuestras variables de entorno en linux vamos a ver ahora cómo podemos cambiar una variable un poco más interesante entre comillas porque es del
sistema paz como estuvimos hablando anteriormente esta variable paz el paz de linux buscaba los ejecutables que tuviéramos dentro de nuestro sistema es decir donde linux encuentra los ejecutables para trabajar y como lo vemos están en estos directorios entonces vamos a hacer ahora un ejemplo muy sencillo vamos a crear un ejecutable y vamos a ver cómo lo podemos poner en el path para que el linux lo encuentre dónde está entonces haciendo b&w vemos que estamos en nuestro usuario home vamos a crear ahora un nuevo directorio
mk dir lo vamos a llamar ejecutable le vamos a dar a inter y nos vamos a nuestro directorio aquí entramos si hacemos un ls espacio menos l comprobamos que está vacío ya que no hemos creado ningún fichero interior entonces ahora vamos a crear un fichero que será esto echo entre comillas ahora colocamos ls espacio menos l cerramos comillas y lo vamos a mandar a un archivo que se va a listar punto s h esto lo veremos más detenidamente más adelante en otros capítulos como ya suponen cuando estemos viendo la sección
de hecho el script pero por definición los script de linux casi siempre terminan en ese h es un poco parecido a los de windows que se llaman punto bat o punto cmd pues aquí entonces las buenas prácticas nos dicen que se deben llamar punto s h pero no es obligatorio al contrario que windows no hace falta que el fichero se llame así pero esta es una buena práctica entonces si aquí le damos a enter y hacemos con él ese espacio - l entonces aquí confirmamos la creación los niveles de permiso el grupo que pertenece etcétera sí aquí hacemos
entonces un catch a nuestro archivo listar punto s h vemos que la instrucción que tiene es un ls menos l que es lo que indicamos acá le mandamos un ls menos l a un archivo que se llama alistar punto s h aquí lo muestre entonces vemos aquí que nuestro lista sh no tiene permisos de ejecución entonces para poner en práctica el tema que ya vimos de la administración de permisos si ejecutamos entonces un ch mod y aquí colocamos más equis y aquí colocamos listar punto s h le estamos diciendo a linux mira mi archivo listar
sh no tiene permisos de ejecución entonces quiero cambiar los permisos de ejecución para listar sh colocándole más le estoy diciendo colocar permisos de ejecución para mí es para mi ejecutable listar punto ch y aquí le doy a enter cuando vuelvo y hago mi ls - l y le doy a enter aquí veo mis x que me aparecen dándome permiso de ejecución para mi usuario para todos los usuarios de mi grupo y para cualquier usuario externo que accede a este archivo entonces aquí empezamos a ver la parte interesante del comando paz si se fijan ahora mismo
tenemos nuestro fichero listar sh que es ejecutable si se fijan ahora nos lo ha cambiado de color verde porque al colocarle los permisos de ejecución cambió el color en el nombre del archivo pero lo más importante es claro que tenga ya los permisos entonces esto es un ejecutable porque va a lanzar un comando pero si yo escribo aquí por ejemplo listar punto s h cuando le doy a enter linux nos dice que no lo encuentra si se preguntan cómo que no lo encuentra si está dentro del usuario que estamos utilizando dentro del directorio bueno
esto es una de las diferencias que tienen linux con windows windows busca en el path también al igual que él y nos en el en las variables de entorno en las variables paz y además en el directorio que nos encontramos entonces esto en linux no es así es decir si yo quiero ejecutar el comando listar sh tendríamos que poner algo como esto barrajón barra y usher 001 barra ejecutable entonces ahora colocamos un barra listar punto s h si yo escribiendo esto le doy a enter funciona porque en realidad lo que hace es un ls

- l no aquí lo tenemos entonces en linux hay que colocar el path completo para que linux pueda acceder a nuestro ejecutable porque linux linux no sabe dónde está nuestro ejecutable entonces otra opción para no colocar el nombre completamente es colocar punto barra recordamos que el punto nos indica el directorio actual esto ya lo vimos anteriormente en nuestra introducción a directorios entonces punto indica directorio actual y cuando colocamos barra listar punto s h al colocar todo esto si le damos a enter ver que el
  linux ejecuta la misma acción es decir procesa nuestro archivo ejecutable porque aquí con el punto barra le estamos diciendo busca el fichero listar sh en el directorio que nos encontramos esto es digamos una especie de truco que tiene linux en el uso de los ejecutables entonces esto nos puede venir muy bien si lo vamos a utilizar una o dos veces pero digamos que lo vamos a utilizar continuamente lo que no andar poniendo es el paz completo cada vez que lo necesitemos porque es bastante tedioso tampoco colocar el
  punto dependiendo del directorio en el que nos encontremos entonces para esto vamos a hacer un clear lo que hacemos es añadirlo al paz esta es la utilidad del path en cualquier sistema operativo si yo aquí hago un echo dólar paz le doy a e ínter comprobamos entonces que el path de mi linux tiene todo esto entonces para cambiar el path es muy sencillo colocamos paz igual a lo que era antes nuestro paz colocando dólar paz cuidado aquí que cuando creamos o cambiamos una variable después de poner el nombre así vamos a
  quitar el set aquí tengo que poner si quiero conservar lo anterior porque si yo me limitará a poner el nuevo directorio estoy perdido habría borrado todo entonces aquí colocó dos puntos para separar el nuevo directorio ahora colocó barra home barra y user 001 barra ejecutable entonces esto es como decirle mira además de lo que ya tenías en el path vas a buscar también en este directorio entonces aquí le doy a enter y cuando hago aquí de nuevo echo dólar paz cuando le doy a enter que vemos aquí que linux ha añadido detrás de lo que
  colocamos en dólar paz el nuevo directorio de nuestro ejecutable entonces si ahora yo escribo aquí por ejemplo lanzar punto s h cuando le doy a enter vemos aquí que me dice orden no encontrada debió localizarlo vamos a hacer un ls a ver qué pasó aclaró es que el comando no es lanzar sino listar entonces me disculpan acá voy a colocarlo de nuevo correctamente listar punto s h y le doy a enter ahora si vemos que nos funciona correctamente simplemente con colocar el nombre de nuestro ejecutable entonces como vemos
  esta es la utilidad de colocar los directorios de nuestros ejecutables en el path de nuestro sistema operativo recuerden que simplemente colocando path igual a dólar paz dos puntos colocamos la ruta del directorio que contiene nuestro ejecutable que en este caso se llama justamente ejecutable ya con esto estamos agregando a nuestro paz nuestro archivo de lanzamiento entonces hacemos eco paz para ver todas las instrucciones que tiene nuestro paz firmamos acá que se agregó correctamente nuestro directorio con nuestro
  ejecutable luego escribimos el nombre de nuestro ejecutable punto sh y simplemente recordando que declaró debemos estar dentro de ese directorio es suficiente para que linux lo ejecute entonces recuerden siempre tener cuidado con esto de no poner aquí dólar paz se habría borrado todo lo anterior es decir para cambiar una variable hay que tener mucha precaución hay que ser muy cuidadoso para no eliminarla y bien eso va a ser todo por ahora en esta parte de las variables en linux trabajando con el path más adelante vamos a continuar
  viendo más y más contenido de este tema de las variables en linux nos vemos en una siguiente entrega
