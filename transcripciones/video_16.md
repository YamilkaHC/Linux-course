hola que tal youtube videntes bienvenidos una vez más a una nueva entrega de este su curso de linux desde cero en el día de hoy vamos a ver unos conceptos muy interesantes a la hora de trabajar con linux pero antes de empezar a trabajar con ellos tenemos que tener muy claro unos conceptos que son distintos y es el conjunto de entrada/salida y errores estándar lo primero que tenemos que tener en cuenta es que para linux todo es un fichero un string una cadena una cadena de caracteres por así decirlo entonces cuando hablamos de ficheros de entrada o de ficheros de entrada y salida estándar siempre pensamos en el teclado en la pantalla en una impresora etcétera pero linux internamente lo que ve en esos dispositivos externos es algo así como una instrucción de tipo string como otro fichero si recuerdan de los primeros capítulos que estuvimos hablando de ficheros teníamos un directorio especial que era el directorio barra pdif que contiene los dispositivos del sistema entonces cuando hablamos de mandar algo dispositivo lo que hace linux mandarlo a uno de esos ficheros a uno de esos dispositivos que tiene en ese directorio bueno entonces en cualquier caso cuando hablamos de entrada salida y error estándar lo que decimos siempre es que dentro de linux hay lo que se denomina una entrada estándar por defecto esta entrada estándar suele ser o el teclado o la salida de otro comando queda fuera del propósito de este curso pero a nivel de administración podemos cambiar este tipo de entradas y salidas de errores estándar pero por defecto si no hay ningún cambio en el sistema de entrada estándar siempre será un teclado el teclado en el que escribimos esta entrada estándar recibe el nombre de std estándar input en inglés y tiene un código de cero que ya veremos más adelante cómo se utiliza bueno pues supongamos que yo tengo un proceso linux del cual requiere una entrada a partir de la entrada estándar por ejemplo unos argumentos dentro de la línea de comandos yo por teclado a través de la entrada estándar de std en esos comandos esas opciones van a entrar en el proceso linux y luego puedo tener digamos dos salidas una salida estándar out std out con el código 1 que normalmente habitualmente es una pantalla o un monitor aunque también puede ser otro fichero como lo veremos a lo largo del curso pero por defecto es una pantalla y luego tenemos otra salida muy interesante y es cuando hay un error ésta se denomina estándar air que tiene código 2 es importante que nos quedemos con estos códigos 0 para entrada estándar 1 para salir de estándar y 2 para salida de error estándar que como vemos también va a un monitor aunque como comentó un administrador podría cambiar incluso las entradas o salidas estándar pero habitualmente casi todos los sistemas que puedan utilizar en el futuro van a ser o bien el teclado o bien la pantalla aunque veremos que también podemos utilizar ficheros como entrada y ficheros como salida pero bueno lo importante de esto es que a lo largo de los siguientes capítulos vamos a utilizar estos conceptos de standard input estándar output y estándar error 0 1 y 2 para poder hacer redirecciones tuberías filtros y otras operaciones muy muy interesantes que conviene que conozcamos dentro del mundo de linux entonces vamos ahora a ver una serie de opciones que nos permiten manipular y trabajar con ese tipo de entrada y salida de las que estuvimos hablando entonces lo primero que tenemos que ver es digamos aquí un ejemplo de lo que hablamos para esto vamos a utilizar el comando eco si yo pongo eco hola lo que estoy haciendo es admitir algo por la entrada estándar el estándar input el teclado y cuando le doy a enter esa entrada estándar se redirige hacia la salida estándar esto es lo que se reconocería o se conoce como estándar input y estándar output hay muchos comandos en linux que esperan recibir algo por la entrada estándar y los redirigen luego a la salida estándar y un típico ejemplo de error sería este por ejemplo si yo coloco catch j y le doy a enter si no existe este fichero lo que hace en linux es devolverme un error el error como hemos visto ya en capítulos anteriores también se redirige automáticamente a la salida estándar entre comillas que es la pantalla entonces vemos que muchos de estos comandos en linux manejan salida estándar entrada estándar y error estándar entonces como hemos visto a lo largo del curso y muchos comandos en lugar de tener entrada estándar tienen normalmente un fichero por ejemplo el comando cut que nos permite visualizar el contenido de un fichero si yo pongo esto catch etc y luego barra ss wv de lo que le estoy pidiendo es que me muestre el contenido dentro de este directorio y aquí lo muestra pero igualmente cat podría también recibir datos de la entrada estándar recuerden que para linux la entrada estándar es un fichero entre comillas entonces si yo coloco catch sin nada vemos que lo que hace es que se queda el cursor en la línea siguiente aquí yo puedo poner por ejemplo hola y cuando le doy a enter lo repite por ejemplo esto es una prueba no por eso en este caso el fichero que está leyendo el comando en este caso es el spd y el estándar input que resulta que es nuestro teclado entonces vemos que esta es una opción muy interesante para ver el comportamiento de la entrada estándar en un fichero que en principio admite otro tipo de entrada como son ficheros normales y corrientes le doy a control de para salir de este tipo de operaciones cuando estoy dentro de la entrada estándar entonces hay otro comando muy interesante que es el comando d esto es translate este comando nos permite cambiar unos caracteres por otros esto puede ser muy útil con ficheros idees y de hecho este comando lo volveremos a utilizar en la sección de shell script pero ahora por ejemplo si yo escribo por ejemplo o lo que le estoy diciendo es que sustituya el carácter por el carácter o si no le pongo ningún fichero lo que va a hacer es pedírmelo por el teclado porque por defecto tr necesita un fichero y si no lo tiene va a pedirlo por el fichero estándar de entrada ltd y entonces sí aquí yo escribo por ejemplo hola cuando le doy a enter vemos aquí que sustituye la a por la o digamos que yo escribo aa cuando le doy a inter fíjense cómo sustituye todos los caracteres a por el carácter o pero lo más interesante no es tanto el comando terenci que es muy interesante de por sí sino que fíjense que yo estoy utilizando la entrada y la salida estándar para manejar ficheros que de otra manera necesitarían tener un fichero como entrada si se preguntan si esto lo van a tener que hacer habitualmente pues normalmente esto no se hace este tipo de operaciones así no se hace de poner un comando y empezar a teclear directamente pero si verán más adelante cuando estemos en el script que esto se usa para hacer programación y ahí verán algunos ejemplos de este tipo de expresiones por lo tanto por ahora lo interesante es tener bien claro lo que es la entrada estándar y la salida estándar entonces vamos a seguir con el concepto de estándar input y estándar output etcétera y vamos a ver ahora una cosa muy muy interesante y que es muy utilizada en niños las re direcciones y que es una redirección pues una redirección no es nada más que digamos la salida o la entrada de algún comando que se dirija a la salida o la entrada estándar esto es muy útil para como usuario como administrador como una de las características más utilizadas con linux vamos a empezar con la primera redirección la primera es redirección es mandar algo que iba a la salida estándar hacia otro sitio y normalmente se utiliza y normal y normalmente no que es el carácter mayor si yo coloco por ejemplo acá vamos a hacer un ejemplo más sencillo digamos eco hola este comando lo que hace como sabemos es permitir la entrada por teclado y mandar el resultado a la pantalla entonces ahora vamos a recuperar un comando que hicimos anteriormente que ya les había dicho que lo íbamos a ver más adelante si yo coloco por ejemplo eco hola y escribo mayor y luego colocó f1 punto de x de aquí lo que le estoy diciendo es que en vez de mandar la salida esperada a la salida estándar lo mandé a un fichero en este caso entonces si yo le doy a internos que no nos saca la palabra hola porque lo ha dejado en un fichero que se llama f1 txt x si aquí entonces yo hago un cat efe 1 punto text4baby yo tengo la palabra hola en mi fichero 1 punto x es decir que la forma más básica de redirección es decir bueno en lugar de mandar a la salida estándar mándala a un fichero y de hecho si yo escribo echo adiós mayor que f1 punto txt y aquí colocó de nuevo catch efe 1 punto p xd veo aquí que lo ha sobre escrito no es decir que el comando mayor lo que hace es que si el fichero no existe lo crea y si el fichero existe lo sustituye entonces pensarán y no se podría añadir cosas claro si yo escribo eco hola y la redirección de añadir que es mayor mayor y escribo efe 1 punto txt le doy a inter y si hago un cut f1 te x te puedo comprobar que en lugar de eso y la palabra que tenía le agrega la nueva palabra hola con el doble mayor mayor si yo pongo por ejemplo gates para que nos devuelva la fecha y le colocó mayor mayor f1 punto txd4 le doy a enter escribo kf1 txd4 le doy a enter puedo ver aquí entonces que nuestro fichero f1 txt tiene las dos palabras que le agregamos anteriormente y aquí nos agrega todo el registro que trae el comando de porque aquí se lo colocamos de doble mayor f1 txt se lo agrega aquí abajo la funcionalidad del comando que es traernos la fecha y la hora del día actualmente entonces digamos aquí que si yo hago por ejemplo cada barra etc barra p ss w me faltaba aquí la a esto es para que me diga los usuarios del sistema y aquí lo veo entonces digamos que si yo en lugar de ponerlo por terminal quiero a mí fichero f txt coloco cada barra etc pss wv de mayor mayor efe 1 punto xd le doy a enter y luego hago mi cut efe 1 punto txt puedo comprobar que tiene también todo el contenido de paz w de además de nuestras líneas de adiós hola y lo que extraje con el comando de bueno entonces con esto de la redirección estaremos realizando muchas prácticas porque a lo largo del curso va a aparecer en muchas ocasiones pero la redirección de salida al final se hace con los símbolos de mayor y mayor mayor y bien eso va a ser todo por el momento en esta introducción a las re direcciones en linux vamos a continuar más adelante nos queda todavía mucho que ver acerca de redirección es pero nos veremos en una siguiente entrega gracias por su atención hasta pronto
