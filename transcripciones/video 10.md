hola que tal youtube videntes bienvenidos una vez más a una nueva entrega de este su curso de linux desde cero en el día de hoy vamos a empezar los enlaces en linux un tema bastante importante al cual vamos a desglosar todo su contenido y que es un enlace en linux bueno pues un enlace en linux es similar a un enlace directo en windows y su principal utilidad es poder acceder a archivos o carpetas de forma más sencilla sin la necesidad de desplazarnos por toda la jerarquía del directorio por ejemplo si necesitamos acceder a una carpeta digamos en este caso documentos tendríamos que usar el comando cd y toda esta ruta para ubicarnos en dicho directorio por lo que es mucho más cómodo crear un enlace en nuestro home que apunte a dicho directorio y así sólo tendríamos que usar el comando cb en linux existen dos tipos de enlaces los enlaces físicos conocidos como heart y los enlaces simbólicos conocidos como soft pero antes de entrar en él los enlaces tenemos que revisar un poco lo que hemos visto respecto a los sistemas de ficheros en linux y para esto vamos a hablar de algo llamado 12 12 es algo muy sencillo cuando el linux se crea un fichero que se sube al sistema de ficheros en realidad lo hace de una forma distinta como lo hace windows ya que lo que hace es asociar lo que se denomina un nodo un in out en inglés y que contiene este nodo bueno este nodo contiene toda la información de metadatos de este fichero no datos sino información de tipo metadatos como por ejemplo tamaño el usuario el grupo el tipo etcétera etcétera y un número de y nodo es decir cuando se crea el sistema de ficheros dentro de linux se crea o se reserva un número de nodos concreto que generalmente es muy alto a cada fichero que se crea se le asigna un número de nodos y además entre la información que contiene el nodo tenemos la dirección de donde se encuentran los bloques de datos del fichero es decir que cuando yo quiero saber o voy a acceder a uno fichero el nodo contiene la información de dónde se encuentran los datos los bloques de datos de ese fichero entonces es como una especie de sitio en donde se guarda toda la información necesaria para ese fichero pero con una pequeña sorpresa y es que el nodo no tiene no guarda dentro de la tabla de nodos del sistema es decir cuando vamos creando ficheros y se les va asignando el nodo estos se van guardando en una tabla de y nodos y no se guarda el nombre del fichero esto es un poco curioso en linux pero es así entonces si nos preguntamos de dónde se saca el nombre del fichero bueno el nombre del fichero lo guardan linux en los directorios es decir los nombres de los ficheros no se guardan en él y nodos y no en los directorios de forma que un determinado directorio por ejemplo este directorio concurso tiene esta estructura si pudiéramos ver dentro de un directorio tendríamos los nombres de los ficheros que tiene ese directorio y el nodo al cual están apuntando de manera que cuando yo quiero encontrar o visualizar o hacer cualquier cosa como el fichero prueba de lo que hace el directorio es asociar leo le asocia el nodo donde se encuentra toda esta información que hayamos visto anteriormente sobre ese fichero tipo tamaño el propietario los bloques de datos etcétera entonces cuando linux arranca carga la tabla de datos de y nodos en memoria y hace la relación final a nivel de directorios es decir que un directorio dentro de linux contiene la lista de los nombres de los ficheros y de los nodos asociados o en realidad del nodo asociado a ese fichero así que como ven es una estructura muy distinta a la de windows pero muy potente podríamos decir y por qué hablamos de esto de nodos y estos objetos bueno porque lo que vamos a ver de aquí en adelante son los enlaces estos enlaces que como referencia tenemos que es lo mismo que enlaces directos en windows y existen dos tipos de enlaces los físicos los enlaces heart y los simbólicos o de tipo soft en los enlaces físicos lo que tenemos son digamos etiquetas o nombres que apuntan al mismo fichero al mismo y nodo entonces yo puedo asociar un solo y nodo que es donde se encuentra la información de un fichero a varios ficheros esto se conoce como enlaces de tipo hard como todos estos enlaces hard apuntan al mismo contenido al mismo y nodo entonces yo puedo jugar yo puedo tener 45 25 enlaces de tipo hard mientras no elimine todos el fichero permanece accesible es decir que todos los enlaces de tipo hard en realidad son el mismo fichero pero con distintos nombres veremos ejemplos de esto como se crean y cómo se manejan más adelante entonces los enlaces de tipo soft son los accesos directos de linux que son iguales a los accesos directos de windows son ficheros independientes que tienen un puntero que apunta al fichero que a la vez apunta al nodo es decir que la diferencia con los de tipo hard es que los de tipo hard son enlaces en sí mismos que apuntan a un y no dos mientras que en los enlaces de tipo soft son ficheros independientes que apuntan a una etiqueta que a la vez apunta a un enlace así que de ahora en adelante pues vamos a empezar a practicar y a mostrar ejemplos de cómo manejar estos objetos en linux y bien de inmediato entonces vamos a poner manos a la obra nos vamos a ir a nuestra terminal y vamos a ver ahora cómo es un enlace de tipo hard para esto vamos a crear un pequeño fichero vamos a estar en nuestro directorio y user 01 voy a hacer esto que dice eco o la mayor fichero 1 punto txt como ya debemos saberlo esto lo que está haciendo es creando un archivo que se llama fichero 1 punto txt que va a contener un texto que dice hola yo le doy a enter y recordamos con el comando catch podemos leer esto que acabamos de colocar aquí me dice que no existe a escribir el nombre incorrecto doy flecha hacia arriba tengo que poner un 1 aquí voy a enter ahora sí aquí está mi palabra hola dentro de mi fichero 1 txt sí aquí hacemos un ls menos el de nuestro fichero 1 punto txt una de las cosas que vimos cuando aprendimos el comando ls es que hay una opción que es este numerito de acá que identifica el número de enlaces que tiene un determinado fichero como vemos aquí en este caso pone 1 solamente ya que este fichero solamente tiene un enlace que es su propio nombre si se acuerdan de lo que estuvimos hablando hace un momento de los nodos de como el nombre en realidad lo que hace es apuntar a un conjunto de digamos bloques de datos si yo hago esto con este comando él y escribo fichero 1 txt y escribo fichero 1 y le pongo un 3 cord enlace punto txt cuando le doy a inter aquí este comando ln lo que hace es crear un enlace que se llama fichero 1 enlace punto txt es un enlace hard de tipo hard sobre mi fichero 1 txt entonces si yo hago ls menos el de ficheros 1 punto x te puedo comprobar aquí ahora que me aparece 12 esto es que el sistema nos dice que este fichero o este bloque de datos al que está apuntando el fichero 1 punto txt tiene otro enlace de hecho si hacemos un ls menos el fichero 1 enlace punto txt cuando le doy a enter veo que también me sale un 2 porque en realidad están apuntando al y no hay una manera muy fácil de saber lo que es utilizando la opción ls menos l y escribo fichero 1 punto txt al yo colocar aquí le estoy diciendo en el sistema que voy a consultar un nodo no colocando ls - lic cuando le doy entonces a enter puedo comprobar entonces que además de los datos que nos da sobre fichero 1 txt nos dice aquí el número del minuto y si yo pongo ls menos l y de fichero 1 andrés cord enlace punto txd4 día entero voy a comprobar aquí que también efectivamente contiene el mismo número de nodo es decir estos dos ficheros fichero 1 y fichero 1 enlace están apuntando a un nodo que es lo que contiene toda la información del fichero el bloque de datos etcétera etcétera aquí nos damos cuenta de que en realidad están apuntando al mismo sitio si quisiéramos tenerlo más claro hacemos por ejemplo un cat fichero 1 punto x de aquí vemos que dice hola y si yo hago por ejemplo echo adiós mayor o mayor fichero 1 punto de x te recordamos que con esto estamos agregando texto al final de mi fichero colocando mayor mayor y aquí por ejemplo le doy a enter y vuelvo y hago un cut a mi fichero 1 punto txt cuando le doy a inter efectivamente aquí pone a dios y si igualmente hago entonces cada fichero 1 a un 3 court enlace cuando le doy a enter puedo confirmar que pone la misma información me trae mis dos palabras colocadas entonces viendo esto en en realidad podemos confirmar que los dos nombres de ficheros están apuntando mediante un enlace hard al y nodo que es el que realmente contiene los datos tales y como lo hemos visto anteriormente voy a limpiar mi terminal digamos que si yo ahora por ejemplo borro con el comando rm mi fichero 1 punto txt cuando le doy a enter no pasaría nada ya que sigo teniendo fichero 1 andrés cord enlace punto txt aquí está pero claramente aquí sólo tengo un link un enlace es decir que mientras mientras a ese y nodo le esté apuntando a algún fichero ese nodo permanece vivo es decir yo puedo tener 24 enlaces duros en la césar a un fichero hasta que no borre los 24 enlaces no se borra realmente el fichero original los datos originales como han visto aquí acabo de hacer un rm de mi fichero 1 txt que fue el primero que creamos pero no pasa nada porque tengo otro que es fichero 1 enlace txt que está apuntando a estos mismos datos y si ahora entonces yo borro mi fichero 1 andrés cord enlace punto txt aquí sí que ya pierdo todos los datos entonces si hago mi ls menos l cuando le doy a inter ya no tengo el fichero 1 ni mi fichero 1 enlace así que con esto comprobamos que los enlaces de tipo hard lo que nos permiten es tener como distintos nombres distintos ficheros que apuntan todos al mismo sitio vamos entonces a limpiar nuestra terminal y vamos a ver ahora los enlaces simbólicos o de tipo soft los enlaces de tipos simbólicos son un poco distintos son más parecidos como ya habíamos dicho a los enlaces directos de windows entonces si yo por ejemplo acá hago un ls menos l si se fijan aquí tenemos un fichero de nuestras lecciones anteriores que se llamaba ejemplo punto txt si van a la par conmigo en el curso también ustedes tienen dentro de su directorio este fichero de ejemplo entonces para crear un enlace simbólico el comando es muy sencillo es igual l n pero en este caso colocamos menos s de simbólico y hacemos lo mismo colocamos nombre del fichero original y ahora nombre del fichero de enlace le damos a enter y hacemos nuestro ls aquí entonces vemos que hay una pequeña diferencia con los enlaces de tipo hard para empezar puedo comprobar que en el primer carácter de tipo de fichero me aparece una l que indica link mientras que en los de enlaces de tipo hard eran ficheros normales aquí nos dice que es un link nos pone una l nos pone el nombre del enlace y vemos que de esta manera nos está indicando a dónde está apuntando el enlace es decir que este enlace en realidad es un acceso directo normal como lo hemos visto en windows como sabemos la diferencia principal entre en la cee heart y un enlace soft es que los enlaces heart apuntan al nodo y son ficheros por sí mismos mientras que un enlace solo es un puntero un apunte al contenido real aquí vemos que pone 11 de tamaño que es diferente al contenido real y si yo aquí hago un rm a mi fichero original cuando le doy a entre iago min ls menos l veo que al contrario de lo que pasaba con los enlaces de tipo hard el enlace de tipo simbólico queda estropeado queda inutilizable vemos que aquí lo pone en color rojo indicando que el fichero original al cual estaba apuntando ha desaparecido por lo tanto aquí si colocamos por ejemplo rm ejemplo andrés cord enlace punto txt lo eliminamos ya que no nos queda más remedio así que como vemos hay opciones o determinadas ocasiones en la que nos interesará utilizar en las este tipo hard y otras en las que vamos a necesitar utilizar enlaces de tipo simbólico y bien eso va a ser todo por ahora referente a los enlaces en linux más adelante seguiremos con más y más temas que tenemos pendientes por aprender acerca de este fascinante sistema operativo nos vemos en una siguiente entrega gracias por su atención y hasta pronto
